name: Update Recent Activity

on:
  schedule:
    - cron: "0 * * * *"   # hourly
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/activity.yml"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Inject recent activity into README
        uses: jamesgeorge007/github-activity-readme@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          GH_USERNAME: hiibrarahmad
          MAX_LINES: 5
          COMMIT_NAME: github-actions[bot]
          COMMIT_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com
          COMMIT_MSG: "chore(readme): refresh recent activity"
          # Broaden filter: include push, create, watch, fork, public events
          # (default excludes everything except PR/Issue/Comment/Release)
          FILTER_EVENTS: ""
